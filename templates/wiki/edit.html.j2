{% extends 'base.html.j2' %}

{% block title %}bearbeiten — {% if namespace == 'wiki' %}{{title}}{% else %}{{namespace}}/{{title}}{% endif %} — GefolgeWiki{% endblock %}

{% block page_content %}
    <h1>
        Wiki-Artikel „{% if namespace == 'wiki' %}{{title}}{% else %}{{namespace}}/{{title}}{% endif %}“ {% if exists %}bearbeiten{% else %}erstellen{% endif %}
        <a href="{{g.view_node.parent.url}}" class="btn btn-danger">Abbrechen</a>
    </h1>
    {{gen_form(wiki_edit_form, g.view_node.url)}}
{% endblock %}
