<div class="col-12 col-md-3" style="text-align: right;">
    {% if g.user.is_authenticated %}
        Angemeldet als {{g.user}}
        <br />
        {% if g.user.is_active %}
            <a href="{{url_for('profile_edit', person=g.user.snowflake)}}">Einstellungen</a> •
        {% endif %}
        <a href="/logout">Abmelden</a>
    {% else %}
        <a href="/login/discord">Anmelden</a>
    {% endif %}
</div>
